// requiring the created models "Thought" and "User"
const User = require('./User');
const Thought = require('./Thought');

// exporting the models "Thought" and "User"
module.exports = { User, Thought };